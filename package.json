{
  "name": "@modelcontextprotocol/server-terminal",
  "version": "0.1.0",
  "description": "MCP server for terminal command execution",
  "license": "MIT",
  "type": "module",
  "bin": {
    "mcp-server-terminal": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "0.5.0",
    "zod": "^3.22.4",
    "zod-to-json-schema": "^3.23.5"
  },
  "devDependencies": {
    "@types/node": "^18.19.115",
    "typescript": "^4.9.5",
    "vitest": "^3.2.4"
  }
}
